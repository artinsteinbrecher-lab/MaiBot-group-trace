# 更新记录

## 0.2.0 - 2026-08-25

本版本基于真实高流量群四轮部署实测迭代，核心指标：时间覆盖从数小时提升到宿主全部可读历史；
关键词命中与数据库独立词统计精确一致；重复查询从约 33 秒降至约 0.4 秒；命令超时清零。

### 新增

- 本地关键词索引：白名单群文本消息实时写入插件 SQLite，寻迹按关键词直查；
  索引未覆盖的更早时段自动分页扫描补齐并回填，越用覆盖越完整。
- 相同查询结果缓存（默认 600 秒，可配置或关闭），命中时标注生成时间。
- 分页扫描完整时间范围，替代单次“最新 N 条”读取；新增“扫描消息上限”配置。
- 扫描水位：到达宿主历史边界后不再重复扫描已到底的时段。
- 寻迹改为后台执行：命令立即确认，结果异步发送，不受宿主命令超时限制。
- 模型调用可配置 RPC 等待时间（默认 120 秒），双通道覆盖宿主默认 30 秒上限。
- 回答末尾附“证据原文”和分行“检索说明”（关键词、原词/扩展命中数、覆盖范围、扫描量）。

### 修复

- 英文/数字关键词按独立词匹配，不再命中其他字母数字串（如 glm 误中 chatglm、网址）。
- 索引直查只用用户原词和模型明确别名，模型改写整句不再拆出泛词污染命中。
- 用户原文始终参与检索；模型编造的排除词（原话中未出现）不再静默删除消息。
- 本地检索不再对长词子片段重复计分；中文滑窗不再跨越“的/和”等连接字。
- 证据裁剪按“离命中消息距离”保留上下文；种子按时间片段分散，不再挤在同一段对话。
- 无效 [E 编号] 从回答正文清除，所有有效引用一律附原文，消除悬空引用。
- 事实整理模型不可用时按文档承诺返回原始证据，不再拒绝展示。
- 监控提醒附带规则编号和暂停提示。
- 证据列表在时间跳跃处插入分段标记，避免不同时间的对话被混为一谈。
- 修复空模型任务在 MaiBot 1.1.0 中可能误路由到 `embedding` 的问题，默认并兜底使用 `utils`。
- 过滤“看看、今天、有没有”等检索口语，避免弱词把无关聊天排到前面。

### 隐私

- 本地索引只覆盖白名单群；移出白名单和超过保留天数（默认 180 天）的消息自动清理；
  功能可整体关闭，详见 `docs/PRIVACY.md`。

## 0.1.0 - 2026-08-15

### 主要功能

- 新增指定群历史寻迹，支持直接文字和回复文字作为线索。
- 新增复合关键词观察，支持 AND、OR、NOT、正则、时间窗口、次数阈值和提醒冷却。
- 新增自然语言规则草稿，必须由创建者二次确认后才会启用。
- 新增本地召回、MaiBot 嵌入重排和带证据编号的事实回答。
- 新增可选通用 JSON 外部资料查询。

### 细节与安全

- 使用 Manifest v2、`MaiBotPlugin`、`PluginConfigBase` 和官方能力代理。
- 使用非阻塞 `ON_MESSAGE` 事件，不阻塞 MaiBot 正常聊天。
- 默认拒绝所有管理命令和群聊读取，必须先配置管理员与群聊白名单。
- 规则、草稿和命中记录保存到插件专属数据目录中的 SQLite。
- 外部查询失败、嵌入失败和模型失败均有明确、可检查的行为。
