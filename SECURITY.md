# 安全说明

## 支持范围

目前只为最新发布版本提供安全修复。使用前请先确认 MaiBot 和 maibot-plugin-sdk 满足 `_manifest.json` 中的版本范围。

## 报告问题

请通过仓库的 Security Advisories 私下报告可能导致越权读取群聊、泄露凭据或绕过管理员白名单的问题。普通缺陷可以直接提交 Issue。

报告时请删除 QQ 登录信息、WebUI Token、模型密钥、搜索 API 密钥和真实群聊内容；可以用脱敏示例说明复现步骤。
